# PoC specification

The immutable working contract is:

`/home/esjung/MLLM-EP-github/poc_flashvep/reports/bottleneck_reproduction_vision_heterogeneous_topk_poc_spec.md`

SHA-256:

`53c27a03dfea4c6c24627cf20118471a6b82659d3e16abbd740e842f5d1d93e0`

The specification was read in full. It is referenced rather than duplicated
so the pre-existing `poc_flashvep/` tree remains untouched and the provenance
of the user-supplied contract stays explicit.
