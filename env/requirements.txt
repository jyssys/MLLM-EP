# Phase 1 CPU environment. Resolve/install with:
#   uv pip compile env/requirements.txt --torch-backend cpu -o env/requirements.lock
#   python -m pip install -r env/requirements.lock
#
# The lockfile includes PyTorch's CPU wheel index because +cpu wheels are not
# discoverable from PyPI alone.
torch==2.12.1
numpy==2.2.6
scipy==1.18.0
pytest==8.4.2
transformers==5.12.1
huggingface-hub==1.20.1
datasets==5.0.0
deepspeed==0.19.2
lmms-eval==0.7.1
pulp==3.3.2
