# Best-candidate live PoC

No candidate qualified for a method prototype. The primary RLR-GV diagnostic
itself was the bounded live mechanism PoC: it executed each rank-local view,
measured proposal exchange, and then executed the exact global path without
allowing the approximation to affect output. Its feasible benefit was 0%.

Per the pre-registered gate, implementing a production replica manager,
topology switcher, approximate layer controller, or custom communication kernel
would be unjustified. This is a gate-closed result, not an environment block.

No live method is implemented below the 12% credible request-level oracle gate.
This file records either the bounded mechanism PoC for the winning candidate or
the measured reason the implementation gate remained closed.
