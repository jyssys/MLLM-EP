# Screening leader: FastPP — not an eligible successor finalist

`BEST_SUCCESSOR_CANDIDATE=FASTPP` is the required exactly-one comparative
selection after all three bounded screens. `FINALIST=NONE`. Scores evaluate
**new successor evidence**, not the quality of the original papers. Zero in a
missing-evidence dimension is not a proof of zero universal opportunity.

FastPP ranks first because dense/native-MoE execution, actual ALP predictor,
four-rank stage identities, independent restart comparisons and a real partition
intervention provide the strongest falsifiable baseline. It does **not** rank
first because a material non-trivial MLLM failure has been found.

| Gate | Result |
|---|---|
| Native original baseline | Dense PP4 and Qwen3 MoE PP4 run; compatibility patches audited |
| Material observed loss | ALP can lose >10% E2E; not unique to MoE or MLLM |
| Simple-fix attack | Existing settings remove that loss; finite additional mean-E2E regret <5% |
| Causal cost-partition test | Positive stage proxy becomes request regression in all three pairs |
| Non-trivial successor oracle | Not established |
| Native MLLM / PP×EP | Port boundary, not measured method failure |
| Kimi / 5-run successor / prototype | NOT_RUN because promotion gates failed |

The 15-engine full-warmup control and six-engine actual partition comparison
are the focused falsification/deep-dive results. They do not justify inventing
a successor mechanism. NanoFlow's subsequent long-output control tests its
setup-amortization limitation, not an independently promoted finalist.

Overall scope is **partial native-transfer coverage**. Do not report all three
methods universally disproven or the full MLLM successor study completed.
Missing exact joint request oracles and ports stay explicit.
