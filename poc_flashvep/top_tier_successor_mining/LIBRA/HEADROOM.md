# Libra headroom — planner evidence is insufficient

## Final GPU resumption supersedes the historical checkpoint below

Final resumption: native48-layer execution and eight real VL groups completed. Internal first-token agreement320/320;HF reference31/32 distinct inputs,so the bridge remains APPROXIMATE. Valid frozen-current-route prediction gain0.146%;actual overlap measured with Nsight. Large no-hook native control passes80/80;large oracle-interleaved run is invalidated. No full-request VL E2E or Kimi claim. See NATIVE_OVERLAP_20260908.md.

## Historical audit / release record

Oracle used so far replaces next-gate predictions with actual future IDs ONLY
inside the original CPU replication/sharding planner. It measures assignment
locality and rank loads, not expert copies, hidden overlap or completion time.

Median locality gain.78125pp is a caution against extrapolating the6–7pp recall
gap into a large speedup. Conversely, it is not a rigorous GPU headroom ceiling:
copy cost, which expert is copied, and execution overlap may be nonlinear.

Wrong prediction cost / missed replication latency: NOT_MEASURED.
Actual overlap and direct request/TTFT gain: NOT_MEASURED.
Quality-matched successor E2E oracle: NOT_MEASURED.
No latency conclusion may use the concurrently measured reduced-layer timings.
