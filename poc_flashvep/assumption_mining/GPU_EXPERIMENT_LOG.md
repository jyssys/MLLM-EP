# GPU experiment authorization log

**Result:** no fresh GPU experiment authorized in this phase.

The contract requires analytical direct request-level headroom before GPU use.
The strongest measured candidate (fixed-shape DeepEP communication debt) has a
1.09% direct request-level cap, while all other measured controls are <=2.5% or
below. The remaining UNKNOWN candidates have no evidence of mass and would
require an instrumentation build before they could be a fair counterfactual.
Running a broad serving sweep would violate the headroom-first rule and would
not be a causal test of a promoted candidate.

The user-requested utilization burn remained active on physical GPUs 1--4;
this is recorded as utilization maintenance, not research measurement.
