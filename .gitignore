# Python and local editor state
__pycache__/
*.py[cod]
.DS_Store
.idea/
.vscode/

# Local environments, credentials, caches, and model/data payloads
.env
.env.*
!.env.example
.venv/
venv/
checkpoints/
activation_dumps/
*.safetensors
*.bin
*.pt
*.pth
*.ckpt

# Vendored upstream snapshots are documented but not duplicated in Git.
external/

# Large profiler binaries. Derived CSV/JSON summaries remain tracked.
*.nsys-rep
*.sqlite
*.qdstrm

# LLaDA2 PoC raw per-rank trace streams are retained locally; compact derived
# CSV/JSON summaries, scripts, reports, and figures are versioned.
poc_llada2_flash_ep/results/**/ep_rank*.jsonl
poc_llada2_flash_ep/results/**/block_rank*.jsonl
poc_llada2_flash_ep/logs/
/results.txt

# RefineGEMM raw 30-repetition operator streams are retained locally. The
# restart-aware medians, correctness rows, audit log and figures are versioned.
poc_refinegemm/kernel*_r*/
poc_refinegemm/smoke_kernel*/
poc_refinegemm/results/**/*.log
poc_refinegemm/results/**/*_gpu.csv

# Superseded or bulky raw profiler streams retained in the local archive.
poc_flashvep/results/phase1b_tp2dp2_smoke_*/
poc_flashvep/results/phase1b_tp2dp2_vision896/audit.jsonl
poc_flashvep/results/phase1b_tp2dp2_vision896/stage_events.jsonl
poc_flashvep/results/tp4_phase1_vision896/stages.jsonl
poc_flashvep/results/tp4_phase1_vision896/lean_stages.jsonl
