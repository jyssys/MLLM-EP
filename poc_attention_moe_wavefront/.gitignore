__pycache__/
*.py[cod]

# Preserve worker-level raw traces locally under the reported result root; the
# checked-in aggregate CSV/JSON tables are sufficient to reproduce figures and
# decisions without adding hundreds of MiB of event objects to git.
results/**/raw/
